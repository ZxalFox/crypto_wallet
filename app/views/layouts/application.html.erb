<!DOCTYPE html>
<html>

<head>
    <title><%= content_for(:title) || "Crypto Wallet" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag :application, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>

<body class="bg-dark text-light">
    <div class="d-flex container-fluid justify-content-around">
        <nav class=" w-75 navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href=" /">Crypto Wallet</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <%= link_to "Moedas", coins_path, class: "nav-link" %>
                        </li>
                        <li>
                            <%= link_to "Histórico de Preços", historical_prices_path, class: "nav-link" %>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="d-flex decoration-none justify-content-around align-items-center">
            <button class="btn  me-3">
                <%= link_to "English", root_path(locale: :en), class: "text-decoration-none text-light" %>
            </button>
            <button class="btn  ">
                <%= link_to "Português", root_path(locale: :'pt-BR'), class: "text-decoration-none text-warning" %>
            </button>
        </div>
    </div>

    <div class="container py-4">
        <%= yield %>
    </div>

    <footer class="footer mt-auto py-3 bg-dark text-center">
        <div class="container">
            <p>
                <%=t('enviroment_message')%> <strong><%= ambiente_rails %></strong>
            </p>
            <span class="text-light">&copy; <%= Time.current.year %> Crypto Wallet</span>
        </div>
    </footer>
</body>

</html>