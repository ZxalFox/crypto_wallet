<%= form_with(model: historical_price) do |form| %>
  <% if historical_price.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(historical_price.errors.count, "error") %> prohibited this historical_price from being saved:</h2>

      <ul>
        <% historical_price.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :coin_id, style: "display: block" %>
    <%= form.text_field :coin_id %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.label :recorded_at, style: "display: block" %>
    <%= form.datetime_field :recorded_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
